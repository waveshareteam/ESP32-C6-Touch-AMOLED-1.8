# Shared Configuration

[简体中文](README_ZH.md)

This directory is reserved for shared ESP-IDF configuration overlays and CI-facing defaults that apply across examples.

Project-specific settings remain in each example directory. Add shared overlays here only when more than one first-party example needs the same setting.

## Markdown audit maintenance

The public Chinese homepage remains `README_CN.md`; the narrow `README_CN.md` pair exception only accommodates that established name. Explicit README pairs and homepage contracts still require reciprocal language links and matching structure.

# 共享配置

[English](README.md)

此目录预留给跨示例共用的 ESP-IDF 配置覆盖和面向 CI 的默认值。

项目专用设置保留在各示例目录；仅当多个第一方示例确实需要同一设置时，才在此添加共享覆盖。

## Markdown 审计维护

公开中文主页继续使用既有的 `README_CN.md` 路径；狭窄的 `README_CN.md` 配对例外仅处理这一命名约定。显式 README 配对和主页契约仍强制双向语言链接与结构一致性。
